[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Scheduled functions
[functions."send-daily-reminder"]
  # Schedule is in UTC (0 16 * * 1-5 = 4:00 PM UTC Mon-Fri)
  schedule = "0 16 * * 1-5"


[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200